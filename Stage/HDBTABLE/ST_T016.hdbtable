column table "ST_T016" (
   "HD"  BINARY(32)     comment 'All attributes except LD, SRC, MANDT, BRSCH',
   "LD"  TIMESTAMP     comment 'NOW()
            ',
   "SRC"  VARCHAR(100)     comment 'S4/HANA',
   "BRSCH"  VARCHAR(4)  NOT NULL   comment 'Industry',
   "BRTXT"  VARCHAR(20)     comment 'Industry Key',
   "MANDT"  VARCHAR(3)  NOT NULL   comment 'Client'
)
comment 'Industries'